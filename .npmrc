# The Nx + Angular 21 + Analog Vitest stack has a peer-dependency tension
# (@analogjs/vitest-angular expects a newer vite than @nx/vite pins). The
# dependency tree resolves correctly in practice; legacy peer resolution makes
# `npm install` and `npm ci` agree, locally and in CI.
legacy-peer-deps=true
