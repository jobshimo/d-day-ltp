# Local neural TTS stack for pre-generating Spanish lesson narration (Kokoro).
# Setup:
#   py -3.12 -m venv tools/tts/.venv
#   tools/tts/.venv/Scripts/python -m pip install -r tools/tts/requirements.txt
# espeak-ng is provided by espeakng-loader (no system install required).
kokoro>=0.9.2
misaki[es]>=0.9.2
soundfile>=0.12
